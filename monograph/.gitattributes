# Checkpoints are not committed (hosted on Hugging Face); keep the rule in case any .pt
# is ever added locally. Figures (*.png) are small and committed as regular blobs — NOT LFS.
*.pt filter=lfs diff=lfs merge=lfs -text
